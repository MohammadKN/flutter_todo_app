name: second_app_todo
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sizer: ^2.0.13

  fluttertoast: ^8.0.7

  flutter_neumorphic:

  cupertino_icons: ^1.0.2

  line_icons: ^2.0.1

  flutter_glow: ^0.2.0
  animations: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon-modified.png"

flutter:
  uses-material-design: true
